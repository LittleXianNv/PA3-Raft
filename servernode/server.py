import time
from threading import Timer
import threading
import metadata

class Server(object):
    def __init__(activeNode):
        self.activeNode = activeNode
        self.metaData = metadata()
    
    def listen_client(self):


    def listen_client(self):
